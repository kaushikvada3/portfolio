# Deploys Portfolio Cinema v2 (index.html + JS modules) to GitHub Pages.
name: Deploy Portfolio to GitHub Pages

on:
  push:
    branches:
      - main
      - eda-tool-theme
    paths:
      - 'index.html'
      - 'graph-fly.js'
      - 'graph.json'
      - 'project-demos.js'
      - 'eggs.js'
      - 'flow-bay.js'
      - '.github/workflows/deploy-portfolio-v2.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Prepare static site
        run: |
          mkdir -p _site
          cp index.html _site/index.html
          cp graph-fly.js _site/graph-fly.js
          cp graph.json _site/graph.json
          cp project-demos.js _site/project-demos.js
          cp eggs.js _site/eggs.js
          cp flow-bay.js _site/flow-bay.js

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
